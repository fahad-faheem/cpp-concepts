#include <iostream>

// if we do not write using namespace std; we will have to write std:: prefix where we use standart (std) namespace components

int main (){




    std::cout << "Look we have to write std:: everywhere" << std::endl;
    std::cout << "Look we have to write std:: everywhere" << std::endl;




    return 0;
}