#include <iostream>         // include to insert, iostream are input output streams (ways)
using namespace std;        // using mean use across whole program, namespace is container for identifiers to avoid name conflict
// std is standard naemspace for C++, with components cout cin vector, without component you will have to use 
//std:: prefix before evry std component 

int main (){     // int is datatype of memory location, main is the function that perform all tasks and this is the main function,

// when the program starts compiling it will look for main function and it will start executing the main function 
// it will do everything what is called for inside main


return 0;  // it is return value After program run succesfully it will return 0, we will have to use ; after line 

}