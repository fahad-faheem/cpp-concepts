#include <iostream>
using namespace std;

int main (){

    // variable 
    // is a space in memory we create, we give values to variablt to hold it and we can use it later
    // we can store value at that space 
    // we access varible with it's number (identifier)
    // we can specify what type of data that varible will hold (int, float, char, bool).


    //syntx to declare variabel (declaration)
    // datatypes name semicollen(;)
    
    int number;

   //syntax to name value (initalization)
   //name = value;

    number = 12;

    // for printing we will only use variable name 

        cout << "This is value of variable: " << number << endl;


   





    return 0;
}